{
  "name": "wiki",
  "version": "1.0.0",
  "private": true,
  "description": "LLM Wiki — personal knowledge base with Wikipedia-style HTML output",
  "scripts": {
    "build": "node build.js",
    "serve": "npm run build && npx -y serve site"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^15.0.0"
  }
}
